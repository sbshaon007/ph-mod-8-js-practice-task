// Task: 18 If the price is greater than 1000, print "Expensive", otherwise "Affordable" — write this using a ternary operator.

const price = 999;

const priceStatus = price > 1000 ? "Expensive" : "Affordable";

console.log(priceStatus);